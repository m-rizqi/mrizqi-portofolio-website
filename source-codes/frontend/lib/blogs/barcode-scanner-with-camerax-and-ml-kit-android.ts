import type { Post } from "../data";

const barcodeScannerWithCameraxAndMlKitAndroid: Post = {
  slug: "barcode-scanner-with-camerax-and-ml-kit-android",
  title: "Barcode Scanner with CameraX and ML Kit — Android",
  tag: "Android",
  date: "Mar 2023",
  readTime: "3 min read",
  excerpt: "Barcodes are ubiquitous in our daily lives, from scanning items at the grocery store to accessing event tickets on our smartphones. With the advent of smartphone cameras and machine learning technology, building a barcode scanner app has become easier than ever before.",
  cover: "/assets/blog/barcode-scanner-with-camerax-and-ml-kit-android/img-1.jpeg",
  link: "https://medium.com/@mrizqi070502/barcode-scanner-with-camerax-and-ml-kit-android-2d540fcd777",
  body: [
    { type: "paragraph", text: "Barcodes are ubiquitous in our daily lives, from scanning items at the grocery store to accessing event tickets on our smartphones. With the advent of smartphone cameras and machine learning technology, building a barcode scanner app has become easier than ever before. In this tutorial, we will explore how to create a barcode scanner app using CameraX, an Android Jetpack library that simplifies camera development, and ML Kit, a machine learning library provided by Google. So let’s get started!" },
    { type: "heading", text: "1️⃣Declare Dependencies" },
    { type: "paragraph", text: "We use CameraX dependencies and ML Kit dependencies. Add those in your `app/build.gradle` ." },
    { type: "code", code: "    // CameraX dependency\n    def camerax_version = \"1.3.0-alpha04\"\n    implementation \"androidx.camera:camera-core:${camerax_version}\"\n    implementation \"androidx.camera:camera-camera2:${camerax_version}\"\n    implementation \"androidx.camera:camera-lifecycle:${camerax_version}\"\n    implementation \"androidx.camera:camera-view:${camerax_version}\"\n    implementation \"androidx.camera:camera-mlkit-vision:${camerax_version}\"\n    implementation \"androidx.camera:camera-extensions:${camerax_version}\"\n\n    // ML Kit\n    implementation 'com.google.android.gms:play-services-mlkit-barcode-scanning:18.1.0'" },
    { type: "paragraph", text: "Actually, there are two types library for the Barcode Scanner from ML Kit." },
    { type: "paragraph", text: "The first one is used if you want to bundling the model in your app." },
    { type: "code", code: "implementation 'com.google.mlkit:barcode-scanning:17.0.3'" },
    { type: "paragraph", text: "The second one is used if you want to use Google Play Service model." },
    { type: "code", code: "implementation 'com.google.android.gms:play-services-mlkit-barcode-scanning:18.1.0'" },
    { type: "paragraph", text: "In this example, we use the second one." },
    { type: "heading", text: "2️⃣Configure Manifest" },
    { type: "paragraph", text: "As the title implies, barcode scanner, we will use the camera and require camera permission." },
    { type: "paragraph", text: "And, if you use the dependency from the Google Play Service in the previous step, you need to add `<meta-data>` like this." },
    { type: "code", code: "<?xml version=\"1.0\" encoding=\"utf-8\"?>\n<manifest xmlns:android=\"http://schemas.android.com/apk/res/android\"\n    xmlns:tools=\"http://schemas.android.com/tools\">\n\n    // Add Camera Permission\n    <uses-permission android:name=\"android.permission.CAMERA\"/>\n\n    <application\n        ....\n        >\n\n        // If you use Google Play Service model, you need to add this metadata\n        <meta-data\n            android:name=\"com.google.mlkit.vision.DEPENDENCIES\"\n            android:value=\"barcode\"/>\n\n        <activity\n            android:name=\".MainActivity\"\n            android:exported=\"true\">\n             ...\n        </activity>\n    </application>\n\n</manifest>" },
    { type: "heading", text: "3️⃣Create The Layout" },
    { type: "paragraph", text: "The layout is simple, just a `FrameLayout` with `PreviewView` inside it. `PreviewView` will display the view from your camera." },
    { type: "code", code: "<?xml version=\"1.0\" encoding=\"utf-8\"?>\n<androidx.constraintlayout.widget.ConstraintLayout xmlns:android=\"http://schemas.android.com/apk/res/android\"\n    xmlns:app=\"http://schemas.android.com/apk/res-auto\"\n    xmlns:tools=\"http://schemas.android.com/tools\"\n    android:layout_width=\"match_parent\"\n    android:layout_height=\"match_parent\"\n    tools:context=\".MainActivity\">\n\n    <FrameLayout\n        android:layout_width=\"match_parent\"\n        android:layout_height=\"match_parent\"\n        android:id=\"@+id/container\"\n        >\n\n        <androidx.camera.view.PreviewView\n            android:id=\"@+id/previewView\"\n            android:layout_width=\"match_parent\"\n            android:layout_height=\"match_parent\"\n            />\n\n    </FrameLayout>\n\n</androidx.constraintlayout.widget.ConstraintLayout>" },
    { type: "heading", text: "4️⃣Request Camera Permission" },
    { type: "paragraph", text: "Next, we request permission to use the camera if we haven’t been granted." },
    { type: "code", code: "class MainActivity : AppCompatActivity() {\n\n    lateinit var previewView : PreviewView\n    val requestPermissionLauncher = registerForActivityResult(ActivityResultContracts.RequestPermission()){\n        // Do logic if not granted\n    }\n\n    override fun onCreate(savedInstanceState: Bundle?) {\n        super.onCreate(savedInstanceState)\n        setContentView(R.layout.activity_main)\n        previewView = findViewById(R.id.previewView)\n\n        if (ContextCompat.checkSelfPermission(this, Manifest.permission.CAMERA) != PackageManager.PERMISSION_GRANTED){\n            requestPermissionLauncher.launch(Manifest.permission.CAMERA)\n        }\n\n    }\n\n}" },
    { type: "heading", text: "5️⃣Get Camera Provider and Set the Listener" },
    { type: "paragraph", text: "`ProcessCameraProvider` is a class that manages the availability of camera devices and provides access to them. Get the `ListenableFuture` from it. The listener will be invoked once the future computation is completed." },
    { type: "code", code: "class MainActivity : AppCompatActivity() {\n\n    lateinit var previewView : PreviewView\n    lateinit var cameraProviderFuture : ListenableFuture<ProcessCameraProvider>\n    val requestPermissionLauncher = registerForActivityResult(ActivityResultContracts.RequestPermission()){\n        // Do logic if not granted\n    }\n\n    override fun onCreate(savedInstanceState: Bundle?) {\n        super.onCreate(savedInstanceState)\n        setContentView(R.layout.activity_main)\n        previewView = findViewById(R.id.previewView)\n\n        if (ContextCompat.checkSelfPermission(this, Manifest.permission.CAMERA) != PackageManager.PERMISSION_GRANTED){\n            requestPermissionLauncher.launch(Manifest.permission.CAMERA)\n        }\n\n        cameraProviderFuture = ProcessCameraProvider.getInstance(this)\n\n        cameraProviderFuture.addListener({\n            val cameraProvider = cameraProviderFuture.get()\n            bindPreview(cameraProvider)\n        }, ContextCompat.getMainExecutor(this))\n    }\n}" },
    { type: "heading", text: "6️⃣Bind Preview" },
    { type: "paragraph", text: "`Preview` is a use case from CameraX to display the view from the camera. Create it by using the builder. Using `CameraSelector` , you can set the lens facing of the camera. Then, set the surface provider into preview. Next bind `ProcessCameraProvider` into the lifecycle." },
    { type: "code", code: "class MainActivity : AppCompatActivity() {\n\n    lateinit var previewView : PreviewView\n    lateinit var cameraProviderFuture : ListenableFuture<ProcessCameraProvider>\n    val requestPermissionLauncher = registerForActivityResult(ActivityResultContracts.RequestPermission()){\n        // Do logic if not granted\n    }\n\n    override fun onCreate(savedInstanceState: Bundle?) {\n        ...\n    }\n\n    private fun bindPreview(cameraProvider: ProcessCameraProvider) {\n        val preview = Preview.Builder()\n            .build()\n        val cameraSelector = CameraSelector.Builder()\n            .requireLensFacing(CameraSelector.LENS_FACING_BACK)\n            .build()\n        preview.setSurfaceProvider(previewView.surfaceProvider)\n        cameraProvider.bindToLifecycle(this as LifecycleOwner, cameraSelector, preview)\n    }\n\n}" },
    { type: "heading", text: "7️⃣Create Analyzer and Read the Value" },
    { type: "paragraph", text: "Now, we come to the barcode scanning part. Create `BarcodeScannerOptions` and specify the barcode format. There are many formats that are provided, which you can read [**here**](https://developers.google.com/ml-kit/vision/barcode-scanning/android#1.-configure-the-barcode-scanner). In this example, I use all formats." },
    { type: "paragraph", text: "Next, create `ImageAnalysis` and set the backpressure strategy `ImageAnalysis.STRATEGY_KEEP_ONLY_LATES` . This strategy guarantees that only one image will be delivered for analysis at a time and will improve real-time performance." },
    { type: "paragraph", text: "Create the `Analyzer` and set it into `ImageAnalysis` object. In this object, you can create the `InputImage` from the `media.Image` ." },
    { type: "paragraph", text: "After that, create a scanner and process the image. Now, you can get the barcode from it." },
    { type: "code", code: "@ExperimentalGetImage\nclass MainActivity : AppCompatActivity() {\n\n    lateinit var previewView : PreviewView\n    lateinit var cameraProviderFuture : ListenableFuture<ProcessCameraProvider>\n    lateinit var executorService: ExecutorService\n    val options = BarcodeScannerOptions.Builder()\n        .setBarcodeFormats(\n            Barcode.FORMAT_ALL_FORMATS\n        )\n        .build()\n    val requestPermissionLauncher = registerForActivityResult(ActivityResultContracts.RequestPermission()){\n        // Do logic if not granted\n    }\n\n    override fun onCreate(savedInstanceState: Bundle?) {\n        super.onCreate(savedInstanceState)\n        setContentView(R.layout.activity_main)\n        previewView = findViewById(R.id.previewView)\n        ...\n\n        executorService = Executors.newSingleThreadExecutor()\n    }\n\n    private fun bindPreview(cameraProvider: ProcessCameraProvider) {\n        val preview = Preview.Builder()\n            .build()\n        val cameraSelector = CameraSelector.Builder()\n            .requireLensFacing(CameraSelector.LENS_FACING_BACK)\n            .build()\n\n        val imageAnalysis = ImageAnalysis.Builder()\n            .setTargetResolution(Size(1280, 720))\n            .setBackpressureStrategy(ImageAnalysis.STRATEGY_KEEP_ONLY_LATEST)\n            .build()\n        imageAnalysis.setAnalyzer(executorService, ImageAnalysis.Analyzer { imageProxy: ImageProxy ->\n            val mediaImage = imageProxy.image\n            if (mediaImage != null){\n                val image = InputImage.fromMediaImage(mediaImage, imageProxy.imageInfo.rotationDegrees)\n                val scanner = BarcodeScanning.getClient(options)\n                scanner.process(image)\n                    .addOnCompleteListener {\n                        if (it.isSuccessful){\n                            for (barcode in it.result) {\n                                val rawValue = barcode.rawValue\n                                Toast.makeText(this, rawValue, Toast.LENGTH_SHORT).show()\n                                val valueType = barcode.valueType\n                                when(valueType){\n                                    Barcode.TYPE_URL -> {\n                                        Intent(Intent.ACTION_VIEW)\n                                            .apply {\n                                                data = Uri.parse(rawValue)\n                                            }\n                                            .also {\n                                                startActivity(it)\n                                            }\n                                    }\n                                    Barcode.TYPE_EMAIL -> {\n                                        Intent(Intent.ACTION_SENDTO)\n                                            .apply {\n                                                data = Uri.parse(\"mailto:\")\n                                                putExtra(Intent.EXTRA_EMAIL, rawValue)\n                                            }\n                                            .also {\n                                                startActivity(it)\n                                            }\n                                    }\n                                    Barcode.TYPE_PHONE -> {\n                                        Intent(Intent.ACTION_SENDTO)\n                                            .apply {\n                                                data = Uri.fromParts(\"tel\", rawValue, null)\n                                            }\n                                            .also {\n                                                startActivity(it)\n                                            }\n                                    }\n                                }\n                                imageProxy.close()\n                            }\n                        }else{\n                            val exception = it.exception\n                            exception?.printStackTrace()\n                            Toast.makeText(this, exception?.message, Toast.LENGTH_SHORT).show()\n                        }\n                        imageProxy.close()\n                    }\n            }else{\n                imageProxy.close()\n            }\n        })\n\n        preview.setSurfaceProvider(previewView.surfaceProvider)\n        cameraProvider.bindToLifecycle(this as LifecycleOwner, cameraSelector, imageAnalysis, preview)\n    }\n\n}" },
    { type: "heading", text: "Fullcode" },
    { type: "code", code: "package com.rizqi.barcodescanner\n\nimport android.Manifest\nimport android.content.Intent\nimport android.content.pm.PackageManager\nimport android.net.Uri\nimport android.os.Bundle\nimport android.util.Size\nimport android.widget.Toast\nimport androidx.activity.result.contract.ActivityResultContracts\nimport androidx.appcompat.app.AppCompatActivity\nimport androidx.camera.core.*\nimport androidx.camera.lifecycle.ProcessCameraProvider\nimport androidx.camera.view.PreviewView\nimport androidx.core.content.ContextCompat\nimport androidx.lifecycle.LifecycleOwner\nimport com.google.common.util.concurrent.ListenableFuture\nimport com.google.mlkit.vision.barcode.BarcodeScannerOptions\nimport com.google.mlkit.vision.barcode.BarcodeScanning\nimport com.google.mlkit.vision.barcode.common.Barcode\nimport com.google.mlkit.vision.common.InputImage\nimport java.util.concurrent.ExecutorService\nimport java.util.concurrent.Executors\n\n@ExperimentalGetImage class MainActivity : AppCompatActivity() {\n\n    lateinit var previewView : PreviewView\n    lateinit var cameraProviderFuture : ListenableFuture<ProcessCameraProvider>\n    lateinit var executorService: ExecutorService\n    val options = BarcodeScannerOptions.Builder()\n        .setBarcodeFormats(\n            Barcode.FORMAT_ALL_FORMATS\n        )\n        .build()\n    val requestPermissionLauncher = registerForActivityResult(ActivityResultContracts.RequestPermission()){\n        // Do logic if not granted\n    }\n\n    override fun onCreate(savedInstanceState: Bundle?) {\n        super.onCreate(savedInstanceState)\n        setContentView(R.layout.activity_main)\n        previewView = findViewById(R.id.previewView)\n\n        if (ContextCompat.checkSelfPermission(this, Manifest.permission.CAMERA) != PackageManager.PERMISSION_GRANTED){\n            requestPermissionLauncher.launch(Manifest.permission.CAMERA)\n        }\n\n        cameraProviderFuture = ProcessCameraProvider.getInstance(this)\n\n        cameraProviderFuture.addListener({\n            val cameraProvider = cameraProviderFuture.get()\n            bindPreview(cameraProvider)\n        }, ContextCompat.getMainExecutor(this))\n\n        executorService = Executors.newSingleThreadExecutor()\n    }\n\n    private fun bindPreview(cameraProvider: ProcessCameraProvider) {\n        val preview = Preview.Builder()\n            .build()\n        val cameraSelector = CameraSelector.Builder()\n            .requireLensFacing(CameraSelector.LENS_FACING_BACK)\n            .build()\n        val imageAnalysis = ImageAnalysis.Builder()\n            .setTargetResolution(Size(1280, 720))\n            .setBackpressureStrategy(ImageAnalysis.STRATEGY_KEEP_ONLY_LATEST)\n            .build()\n        imageAnalysis.setAnalyzer(executorService, ImageAnalysis.Analyzer { imageProxy: ImageProxy ->\n            val mediaImage = imageProxy.image\n            if (mediaImage != null){\n                val image = InputImage.fromMediaImage(mediaImage, imageProxy.imageInfo.rotationDegrees)\n                val scanner = BarcodeScanning.getClient(options)\n                scanner.process(image)\n                    .addOnCompleteListener {\n                        if (it.isSuccessful){\n                            for (barcode in it.result) {\n                                val rawValue = barcode.rawValue\n                                Toast.makeText(this, rawValue, Toast.LENGTH_SHORT).show()\n                                val valueType = barcode.valueType\n                                when(valueType){\n                                    Barcode.TYPE_URL -> {\n                                        Intent(Intent.ACTION_VIEW)\n                                            .apply {\n                                                data = Uri.parse(rawValue)\n                                            }\n                                            .also {\n                                                startActivity(it)\n                                            }\n                                    }\n                                    Barcode.TYPE_EMAIL -> {\n                                        Intent(Intent.ACTION_SENDTO)\n                                            .apply {\n                                                data = Uri.parse(\"mailto:\")\n                                                putExtra(Intent.EXTRA_EMAIL, rawValue)\n                                            }\n                                            .also {\n                                                startActivity(it)\n                                            }\n                                    }\n                                    Barcode.TYPE_PHONE -> {\n                                        Intent(Intent.ACTION_SENDTO)\n                                            .apply {\n                                                data = Uri.fromParts(\"tel\", rawValue, null)\n                                            }\n                                            .also {\n                                                startActivity(it)\n                                            }\n                                    }\n                                }\n                                imageProxy.close()\n                            }\n                        }else{\n                            val exception = it.exception\n                            exception?.printStackTrace()\n                            Toast.makeText(this, exception?.message, Toast.LENGTH_SHORT).show()\n                        }\n                        imageProxy.close()\n                    }\n            }else{\n                imageProxy.close()\n            }\n        })\n        preview.setSurfaceProvider(previewView.surfaceProvider)\n        cameraProvider.bindToLifecycle(this as LifecycleOwner, cameraSelector, imageAnalysis, preview)\n    }\n\n}" },
    { type: "heading", text: "Result" },
    { type: "image", src: "/assets/blog/barcode-scanner-with-camerax-and-ml-kit-android/img-1.jpeg" },
    { type: "paragraph", text: "Result of the code" },
    { type: "paragraph", text: "Yay 🎉, we just learn how to scan barcode/QR code using CameraX and ML Kit library. Thanks for following this far. [**Follow me**](https://medium.com/@mrizqi070502/about) to get more about Android Development posts. Let’s connect on [**LinkedIn**](https://www.linkedin.com/in/m-rizqi/) and [**Instagram**](https://www.instagram.com/_riz.qi/)" },
    { type: "heading", text: "Reference" },
    {
      type: "links",
      items: [
        { label: "Scan barcodes with ML Kit on Android | Google Developers", href: "https://developers.google.com/ml-kit/vision/barcode-scanning/android" },
        { label: "CameraX architecture | Android Developers", href: "https://developer.android.com/training/camerax/architecture" },
        { label: "Implement a preview | Android Developers", href: "https://developer.android.com/training/camerax/preview?" },
        { label: "Image analysis | Android Developers", href: "https://developer.android.com/training/camerax/analyze" },
        { label: "Qr Scanner app | Google’s ML Kit 2021 | Android Studio 2021 by Android Coding Time", href: "https://youtu.be/iryHXuwuJ3Q" },
      ],
    },
  ],
};

export default barcodeScannerWithCameraxAndMlKitAndroid;
