import type { Post } from "../data";

const pictureInPictureModeInAndroidApp: Post = {
  slug: "picture-in-picture-mode-in-android-app",
  title: "Picture-in-Picture Mode in Android App",
  tag: "Android",
  date: "Feb 2023",
  readTime: "3 min read",
  excerpt: "Picture-in-Picture is a feature in Android that lets you see the content of the app will minimize it and do other tasks on your phone. This feature is commonly found in apps that play videos. Using this feature, You can still watch the video through a little square floating window. This feature has been around since Android version 8.",
  cover: "/assets/blog/picture-in-picture-mode-in-android-app/img-1.gif",
  link: "https://medium.com/@mrizqi070502/picture-in-picture-mode-in-android-app-a65a60b5b094",
  body: [
    { type: "heading", text: "🧐What is Picture-in-Picture (Pip) ?" },
    { type: "paragraph", text: "Picture-in-Picture is a feature in Android that lets you see the content of the app will minimize it and do other tasks on your phone. This feature is commonly found in apps that play videos. Using this feature, You can still watch the video through a little square floating window. This feature has been around since Android version 8." },
    { type: "heading", text: "✅Steps" },
    { type: "heading", text: "Set Up the Android Manifest" },
    { type: "paragraph", text: "Because android system not supporting pip mode by default, we need to add the **android:supportsPictureInPicture** attribute into the activity." },
    { type: "paragraph", text: "Then, add the **android:configChanges** attribute to tell the system that the activity handles configuration changes. So, the activity doesn’t relaunch during the pip transition." },
    { type: "code", code: "<?xml version=\"1.0\" encoding=\"utf-8\"?>\n<manifest xmlns:android=\"http://schemas.android.com/apk/res/android\"\n    xmlns:tools=\"http://schemas.android.com/tools\">\n\n    <application\n        ...\n        >\n        <activity\n            android:name=\".MainActivity\"\n            android:exported=\"true\"\n            // Add this 3 attributes\n            android:supportsPictureInPicture=\"true\"\n            android:configChanges=\"screenSize|smallestScreenSize|screenLayout|orientation\"\n            >\n            ...\n        </activity>\n    </application>\n\n</manifest>" },
    { type: "heading", text: "Create the Fullscreen Layout" },
    { type: "paragraph", text: "The layout is simple, consists of VideoView with two ImageButton on top of it. One for play or pause, and the other for entering the pip mode." },
    { type: "code", code: "<?xml version=\"1.0\" encoding=\"utf-8\"?>\n<androidx.constraintlayout.widget.ConstraintLayout xmlns:android=\"http://schemas.android.com/apk/res/android\"\n    xmlns:app=\"http://schemas.android.com/apk/res-auto\"\n    xmlns:tools=\"http://schemas.android.com/tools\"\n    android:layout_width=\"match_parent\"\n    android:layout_height=\"match_parent\"\n    tools:context=\".MainActivity\"\n    >\n\n    <VideoView\n        android:id=\"@+id/videoView\"\n        android:layout_width=\"match_parent\"\n        android:layout_height=\"wrap_content\"\n        app:layout_constraintEnd_toEndOf=\"parent\"\n        app:layout_constraintStart_toStartOf=\"parent\"\n        app:layout_constraintTop_toTopOf=\"parent\"\n        />\n\n    <ImageButton\n        android:id=\"@+id/buttonPlayOrPause\"\n        android:layout_width=\"28dp\"\n        android:layout_height=\"wrap_content\"\n        app:layout_constraintBottom_toBottomOf=\"@id/videoView\"\n        app:layout_constraintStart_toStartOf=\"parent\"\n        android:layout_marginStart=\"24dp\"\n        android:layout_marginBottom=\"16dp\"\n        android:src=\"@drawable/ic_baseline_play_arrow_24\"\n        android:background=\"#66000000\"\n        />\n\n    <ImageButton\n        android:id=\"@+id/buttonEnterPIPMode\"\n        android:layout_width=\"28dp\"\n        android:layout_height=\"wrap_content\"\n        app:layout_constraintBottom_toBottomOf=\"@id/videoView\"\n        app:layout_constraintEnd_toEndOf=\"parent\"\n        android:layout_marginBottom=\"16dp\"\n        android:layout_marginEnd=\"24dp\"\n        android:src=\"@drawable/ic_baseline_fullscreen_24\"\n        android:background=\"#66000000\"\n        />\n\n</androidx.constraintlayout.widget.ConstraintLayout>" },
    { type: "heading", text: "Set Up Video Functionality" },
    { type: "paragraph", text: "Using VideoView, we can just add the video uri and then just play or pause the video using it. I also change the icon when the video is changed to played or paused." },
    { type: "code", code: "class MainActivity : AppCompatActivity() {\n    private lateinit var videoView: VideoView\n    private lateinit var enterPIPModeButton : ImageButton\n    private lateinit var playOrPauseButton : ImageButton\n\n    override fun onCreate(savedInstanceState: Bundle?) {\n        super.onCreate(savedInstanceState)\n        setContentView(R.layout.activity_main)\n        supportActionBar?.hide()\n\n        videoView = findViewById(R.id.videoView)\n        enterPIPModeButton = findViewById(R.id.buttonEnterPIPMode)\n        playOrPauseButton = findViewById(R.id.buttonPlayOrPause)\n\n        val uri = Uri.parse(\"android.resource://$packageName/+${R.raw.video}\")\n        videoView.setVideoURI(uri)\n        videoView.start()\n\n        playOrPauseButton.setOnClickListener {\n            playOrPauseVideo()\n        }\n\n    }\n\n    @SuppressLint(\"UseCompatLoadingForDrawables\")\n    private fun playOrPauseVideo(){\n        if (videoView.isPlaying){\n            videoView.pause()\n        } else {\n            videoView.start()\n        }\n        playOrPauseButton.setImageDrawable(if (videoView.isPlaying) getDrawable(R.drawable.ic_baseline_pause_24) else getDrawable(R.drawable.ic_baseline_play_arrow_24))\n    }\n\n}\n" },
    { type: "heading", text: "Create Pip Params" },
    { type: "paragraph", text: "PictureInPictureParams describe how the pip works. Inside the builder, you can specify some configurations like the aspect ratio, actions, rectangle hint, and many more." },
    { type: "paragraph", text: "**setSourceRectHint()** method makes the entering pip animation works smoothly by indicating the area that is visible following the transition." },
    { type: "paragraph", text: "To process the action, we can use BroadcastReceiver, Service, or Activity by creating PendingIntent and passing it into RemoteAction." },
    { type: "code", code: "class MainActivity : AppCompatActivity() {\n    ...\n\n    private val pipParamsBuilder by lazy {\n        if (Build.VERSION.SDK_INT >= Build.VERSION_CODES.O) {\n            PictureInPictureParams.Builder()\n                .setAspectRatio(Rational(16, 9))\n                .setActions(\n                    listOf(\n                        RemoteAction(\n                            Icon.createWithResource(this,\n                                if (videoView.isPlaying) R.drawable.ic_baseline_pause_24 else R.drawable.ic_baseline_play_arrow_24\n                            ),\n                            \"Action\",\n                            \"Action\",\n                            PendingIntent.getBroadcast(\n                                applicationContext,\n                                1,\n                                Intent(applicationContext, PipReceiver::class.java),\n                                PendingIntent.FLAG_IMMUTABLE\n                            )\n                        )\n                    )\n                )\n        } else {\n            null\n        }\n    }\n\n    override fun onCreate(savedInstanceState: Bundle?) {\n        ...\n        videoView.addOnLayoutChangeListener { v, left, top, right, bottom, oldLeft, oldTop, oldRight, oldBottom ->\n            val sourceRectHint = Rect()\n            videoView.getGlobalVisibleRect(sourceRectHint)\n            if (Build.VERSION.SDK_INT >= Build.VERSION_CODES.O) {\n                setPictureInPictureParams(\n                    pipParamsBuilder\n                        !!.setSourceRectHint(sourceRectHint)\n                        .build()\n                )\n            }\n        }\n    }\n\n    ...\n\n}" },
    { type: "code", code: "class PipReceiver : BroadcastReceiver(){\n    override fun onReceive(context: Context?, intent: Intent?) {\n        Toast.makeText(context, \"Action Clicked\", Toast.LENGTH_SHORT).show()\n    }\n}" },
    { type: "code", code: "<receiver android:name=\".PipReceiver\"/>" },
    { type: "heading", text: "Listen for Picture-in-Picture Mode Changes" },
    { type: "paragraph", text: "This step is optional. But if you want to do some logic when the pip mode is changed, you can do it here." },
    { type: "code", code: "override fun onPictureInPictureModeChanged(\n        isInPictureInPictureMode: Boolean,\n        newConfig: Configuration\n    ) {\n        if (Build.VERSION.SDK_INT >= Build.VERSION_CODES.O) {\n            super.onPictureInPictureModeChanged(isInPictureInPictureMode, newConfig)\n        }\n        if (isInPictureInPictureMode){\n            playOrPauseButton.visibility = View.INVISIBLE\n            enterPIPModeButton.visibility = View.INVISIBLE\n        }else{\n            playOrPauseButton.visibility = View.VISIBLE\n            enterPIPModeButton.visibility = View.VISIBLE\n        }\n    }" },
    { type: "heading", text: "Enter the Pip Mode" },
    { type: "paragraph", text: "To enter pip mode, you can call **enterPictureInPictureMode()** while passing the Pip Params that we created before." },
    { type: "paragraph", text: "**onUserLeaveHint()** is a callback method that is called when the user leaves the app by pressing the home button. In this method, I also make the app enter pip mode" },
    { type: "code", code: "enterPIPModeButton.setOnClickListener {\n            if (Build.VERSION.SDK_INT >= Build.VERSION_CODES.O) {\n                enterPictureInPictureMode(pipParamsBuilder!!.build())\n            }\n        }" },
    { type: "code", code: "override fun onUserLeaveHint() {\n        if (Build.VERSION.SDK_INT >= Build.VERSION_CODES.O) {\n            enterPictureInPictureMode(pipParamsBuilder!!.build())\n        }\n    }" },
    { type: "heading", text: "Full Code" },
    { type: "code", code: "package com.rizqi.pictureinpicture\n\nimport android.annotation.SuppressLint\nimport android.app.PendingIntent\nimport android.app.PictureInPictureParams\nimport android.app.RemoteAction\nimport android.content.BroadcastReceiver\nimport android.content.Context\nimport android.content.Intent\nimport android.content.res.Configuration\nimport android.graphics.Rect\nimport android.graphics.drawable.Icon\nimport android.net.Uri\nimport android.os.Build\nimport androidx.appcompat.app.AppCompatActivity\nimport android.os.Bundle\nimport android.util.Rational\nimport android.view.View\nimport android.widget.Button\nimport android.widget.ImageButton\nimport android.widget.Toast\nimport android.widget.VideoView\nimport androidx.constraintlayout.widget.ConstraintLayout\n\nclass MainActivity : AppCompatActivity() {\n    private lateinit var videoView: VideoView\n    private lateinit var enterPIPModeButton : ImageButton\n    private lateinit var playOrPauseButton : ImageButton\n\n    private val pipParamsBuilder by lazy {\n        if (Build.VERSION.SDK_INT >= Build.VERSION_CODES.O) {\n            PictureInPictureParams.Builder()\n                .setAspectRatio(Rational(16, 9))\n                .setActions(\n                    listOf(\n                        RemoteAction(\n                            Icon.createWithResource(this,\n                                if (videoView.isPlaying) R.drawable.ic_baseline_pause_24 else R.drawable.ic_baseline_play_arrow_24\n                            ),\n                            \"Action\",\n                            \"Action\",\n                            PendingIntent.getBroadcast(\n                                applicationContext,\n                                1,\n                                Intent(applicationContext, PipReceiver::class.java),\n                                PendingIntent.FLAG_IMMUTABLE\n                            )\n                        )\n                    )\n                )\n        } else {\n            null\n        }\n    }\n\n    override fun onCreate(savedInstanceState: Bundle?) {\n        super.onCreate(savedInstanceState)\n        setContentView(R.layout.activity_main)\n        supportActionBar?.hide()\n\n        videoView = findViewById(R.id.videoView)\n        enterPIPModeButton = findViewById(R.id.buttonEnterPIPMode)\n        playOrPauseButton = findViewById(R.id.buttonPlayOrPause)\n\n        val uri = Uri.parse(\"android.resource://$packageName/+${R.raw.video}\")\n        videoView.setVideoURI(uri)\n        videoView.start()\n\n        videoView.addOnLayoutChangeListener { v, left, top, right, bottom, oldLeft, oldTop, oldRight, oldBottom ->\n            val sourceRectHint = Rect()\n            videoView.getGlobalVisibleRect(sourceRectHint)\n            if (Build.VERSION.SDK_INT >= Build.VERSION_CODES.O) {\n                setPictureInPictureParams(\n                    pipParamsBuilder\n                        !!.setSourceRectHint(sourceRectHint)\n                        .build()\n                )\n            }\n        }\n\n        playOrPauseButton.setOnClickListener {\n            playOrPauseVideo()\n        }\n\n        enterPIPModeButton.setOnClickListener {\n            if (Build.VERSION.SDK_INT >= Build.VERSION_CODES.O) {\n                enterPictureInPictureMode(pipParamsBuilder!!.build())\n            }\n        }\n    }\n\n    @SuppressLint(\"UseCompatLoadingForDrawables\")\n    private fun playOrPauseVideo(){\n        if (videoView.isPlaying){\n            videoView.pause()\n        } else {\n            videoView.start()\n        }\n        playOrPauseButton.setImageDrawable(if (videoView.isPlaying) getDrawable(R.drawable.ic_baseline_pause_24) else getDrawable(R.drawable.ic_baseline_play_arrow_24))\n    }\n\n    override fun onUserLeaveHint() {\n        if (Build.VERSION.SDK_INT >= Build.VERSION_CODES.O) {\n            enterPictureInPictureMode(pipParamsBuilder!!.build())\n        }\n    }\n\n    override fun onPictureInPictureModeChanged(\n        isInPictureInPictureMode: Boolean,\n        newConfig: Configuration\n    ) {\n        if (Build.VERSION.SDK_INT >= Build.VERSION_CODES.O) {\n            super.onPictureInPictureModeChanged(isInPictureInPictureMode, newConfig)\n        }\n        if (isInPictureInPictureMode){\n            playOrPauseButton.visibility = View.INVISIBLE\n            enterPIPModeButton.visibility = View.INVISIBLE\n        }else{\n            playOrPauseButton.visibility = View.VISIBLE\n            enterPIPModeButton.visibility = View.VISIBLE\n        }\n    }\n\n}\n\nclass PipReceiver : BroadcastReceiver(){\n    override fun onReceive(context: Context?, intent: Intent?) {\n        Toast.makeText(context, \"Action Clicked\", Toast.LENGTH_SHORT).show()\n    }\n}" },
    { type: "heading", text: "📱Result" },
    { type: "image", src: "/assets/blog/picture-in-picture-mode-in-android-app/img-1.gif" },
    { type: "paragraph", text: "Result after implementing pip mode" },
    { type: "paragraph", text: "Yay 🎉, you just learn how to implement Picture-in-Picture mode in Android. Thanks for following this far. [**Follow me**](https://medium.com/@mrizqi070502/about) to get more about Android Development posts. Let’s connect on [**LinkedIn**](https://www.linkedin.com/in/m-rizqi/) and [**Instagram**](https://www.instagram.com/_riz.qi/)" },
    { type: "heading", text: "Reference" },
    {
      type: "links",
      items: [
        { label: "Picture-in-picture (PiP) support | Android Developers", href: "https://developer.android.com/develop/ui/views/picture-in-picture" },
        { label: "How to Implement a Floating Window in Android (Picture-in-Picture Mode) — Android Studio Tutorial — YouTube", href: "https://www.youtube.com/watch?v=_CDxUCHaQWM" },
      ],
    },
  ],
};

export default pictureInPictureModeInAndroidApp;
